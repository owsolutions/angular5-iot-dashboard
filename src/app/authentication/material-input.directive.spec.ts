import { MaterialInputDirective } from './material-input.directive';

describe('MaterialInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialInputDirective();
    expect(directive).toBeTruthy();
  });
});
